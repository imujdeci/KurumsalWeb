@model IEnumerable<KurumsalWeb.Models.Hakkimizda>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Hakkımıza</h2>

<p>
    <a  class="btn btn-default " href="/Hakkimizda/Create/">Yeni Oluştur</a> @*@Html.ActionLink("Yeni Oluştur", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Aciklama)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Aciklama)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.HakkimizdaId }) |
            @Html.ActionLink("Detaylar", "Details", new { id=item.HakkimizdaId }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.HakkimizdaId })
        </td>
    </tr>
}

</table>
